describe('account service', () => {
  afterAll(() => {
    jest.resetAllMocks();
    jest.clearAllMocks();
  });

  describe('create', () => {
    it.todo('create a new account with profile');
    it.todo('fails by not sending required information');
    it.todo(
      'fails by trying to create an account with unique fields already in use'
    );
    it.todo(
      'fails by trying to create an account with unique fields already in use'
    );
  });
});
